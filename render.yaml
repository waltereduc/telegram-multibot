services:
  - type: web
    name: telegram-multibot
    env: python
    region: frankfurt
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python app.py"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9  # ← КЛЮЧЕВОЕ ИЗМЕНЕНИЕ
